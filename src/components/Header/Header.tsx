import React, { FC } from 'react';

interface Props {
  message: string;
}

const Header = () => {
  return (
		<header className="header">
			<div className="">header</div>
		</header>
	);
};

export default Header;